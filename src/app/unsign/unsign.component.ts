import { Component } from '@angular/core';

@Component({
  selector: 'unsign-pages',
  template: `
    <unsign-layout>
      <router-outlet></router-outlet>
    </unsign-layout>
  `,
})
export class UnsignComponent {
}
